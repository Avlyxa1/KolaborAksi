# Database (Docker Compose MySQL)
DATABASE_URL=mysql://root:password@localhost:3306/kolaboraksi_dev

# JWT
JWT_SECRET=
JWT_REFRESH_SECRET=

# SMTP (Nodemailer)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Firebase Admin SDK (JSON credential — JANGAN commit ke repo)
FIREBASE_SERVICE_ACCOUNT_KEY=

# Server
PORT=3000
